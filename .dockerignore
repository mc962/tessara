# Root .dockerignore — used by apps/tessera-server/deployment/Dockerfile.dev,
# whose build context is the whole workspace (it builds tessera-core from
# local source via the uv workspace, not a published package). The
# production Dockerfile uses apps/tessera-server/.dockerignore instead — its
# context is just that directory.

# Version control
.git
.gitignore

# Python
__pycache__
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info
build
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov/

# Virtual environments
.venv
venv
env
ENV

# Node — reinstalled/rebuilt in the frontend-builder stage
node_modules
apps/tessera-server/static/js/dist

# IDE
.vscode
.idea
*.swp
*.swo
*~
.DS_Store
*.iml

# Not needed to build/run the server image
apps/tessera-cli
docs/
*.md

# (deployment/ itself is kept — docker-entrypoint.sh lives there and is
# COPYed as part of apps/tessera-server; it's tiny)

# Local data / secrets — never bake these into an image
apps/tessera-server/data/
apps/tessera-server/.env
apps/tessera-server/.env.*

# Tests
apps/tessera-server/tests/
packages/tessera-core/tests/
